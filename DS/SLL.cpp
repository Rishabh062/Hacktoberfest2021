#include<stdio.h>
struct SLL
{
    int data;
    struct node *next;
}one;

struct node *head,*newnode;
int main(){
head=0;
newnode=(node*)malloc(sizeof(node*));
printf("\n Enter data : ");
scanf("%d",&newdata->data);
newnode->next=0;
}

